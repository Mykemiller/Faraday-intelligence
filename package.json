{
  "name": "faraday-dashboard",
  "version": "1.0.1",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:zip": "vite build && rm -f faraday-upload.zip && (cd dist && zip -r ../faraday-upload.zip .)"
  }
}
